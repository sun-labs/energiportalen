import chai from 'chai';
const should = chai.should();

describe('[FE] Placeholder tests', () => {

  before(() => { // run before this section

  });

  beforeEach(() => { // run before each test

  });

  it('Checks if tests work', (done) => {
    true.should.equal.true;
    done();
  });

});